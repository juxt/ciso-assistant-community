# Meridian advisor review — 2026-05-23T13:54:48

Target: `https://localhost:8443`
Persona: Sarah Chen, Director of AI Governance, Lorem Ipsum Wealth Partners
Scenario: reviewing the Meridian AI Scenario Advisor submission, Allium spec in the loop.
Submission is retrieved from RAG (not pasted inline).

## Exchange 1

### Sarah

Review the Meridian AI Scenario Advisor submission against our AI governance spec. Walk me through proposed tier, asserted controls, the HITL arrangement, and any blocking findings you would raise. Cite the spec by rule or invariant name.

### Copilot

## Meridian AI Scenario Advisor — Conformance Review (AI-RA-2026-072)

---

### ⚠️ Critical Gap: Key Documents Not in Context

Before findings, I must flag a retrieval problem. My context contains only **Section 1 (System Summary)** of the submission and partial snippets of the prose policy (LIWP-POL-AI-001). The following are **absent**:

| Missing Document | Why It Matters |
|---|---|
| Full submission (Sections 2 onwards) | Tier classification, control assertions, HITL design, data flows, model card — all the material I would check against the spec |
| Allium spec (`.allium` file — "AI Governance Policy — Executable Specification") | The executable `rule`, `invariant`, and `entity` definitions that govern conformance checks |

I cannot fabricate rule names, invariant text, or submission assertions I have not read. Doing so would be a false review. I will not do that.

**Recommended immediate action:** Confirm both documents are ingested and indexed in CISO Assistant's library, then re-trigger retrieval (or call `search_library` explicitly for `AI-RA-2026-072` and the Allium spec file). Once they surface, I can run the full conformance pass.

---

### What I Can Confirm From Retrieved Context

#### Section 1 — System Summary (as retrieved)

| Field | Value |
|---|---|
| Submission ref | AI-RA-2026-072 |
| System name | Meridian AI Scenario Advisor (codename: Lighthouse) |
| Sponsor | David Tanaka, Head of Equity Derivatives Risk, Markets Division |
| Technical owner | Priya Sundaram, Quant Engineering Lead |
| Model | Anthropic `claude-sonnet-4-6`, direct API |
| Stage | Built and tested; pilot June 2026 |
| Users | ~25 risk analysts, New York and London |
| Client population | **Truncated in retrieval — not available** |

**Note on client population:** The client-facing scope field was cut off in the retrieved snippet. This is material: LIWP-POL-AI-001 §7.1 triggers additional obligations (Regulation Best Interest, documented fee-comparison surface) for any system that "produces or shapes recommendations on which clients may rely." I cannot determine whether §7.1 applies without seeing the complete client population field and the system's output design.

---

#### Preliminary Observations Against Prose Policy

These are observations against the *prose policy only* — I am not in a position to cite spec rules without the Allium file.

1. **HITL (LIWP-POL-AI-001 §8):** The policy requires that any Risk Tier mandating HITL must describe the arrangement in sufficient detail — specifically the *time available for review* and *information presented at the point of review* — so that the AI Governance Office can assess whether the human reviewer is in a position to make an informed judgment. I cannot verify whether the submission meets this standard because the relevant section is not in context.

2. **Direct API usage:** The submission notes Anthropic `claude-sonnet-4-6` via direct API. Depending on how the spec classifies vendor API integrations (data residency, model versioning stability, third-party dependency), this may implicate controls I cannot name without the Allium file.

3. **Pilot timeline pressure:** Pilot is scheduled June 2026 — approximately three to four weeks from submission date of 19 May 2026. If the spec encodes a mandatory review window (a common construct in executable governance specs), the timeline is tight. I cannot confirm or deny a specific cooling-off rule without the spec.

---

### What I Need to Proceed

To deliver the complete review you asked for — proposed tier, asserted controls, HITL conformance, and blocking findings — please ensure the following are retrievable:

1. **Full submission AI-RA-2026-072** (all sections beyond the system summary)
2. **Allium spec** — "AI Governance Policy — Executable Specification"

Once those are in context, I will structure the review as: tier classification check → per-control conformance table → HITL invariant analysis → blocking vs. non-blocking findings → recommended reviewer action. All citations will be to named spec constructs.

<details><summary>thinking</summary>

The user wants a comprehensive review of the Meridian AI Scenario Advisor submission against the AI governance spec. I have some context from the vector search, but I need more detail from both the submission and the Allium spec. Let me work with what I have in context and note where information is missing.

From the context I can see:
- The submission is AI-RA-2026-072 for Meridian AI Scenario Advisor v1.0
- Business sponsor: David Tanaka, Head of Equity Derivatives Risk
- Technical owner: Priya Sundaram
- Vendor: Anthropic, claude-sonnet-4-6 (direct API)


- Stage: Built and tested; pilot scheduled June 2026
- User population: ~25 risk analysts (NY and London)

I'm realizing I don't have the full submission details beyond Section 1—I'm missing the tier classification, control requirements, and HITL arrangement specifics. I also don't have access to the Allium spec itself to compare against. I need to search for these documents to provide a proper review. I can see the submission has a system summary in Section 1, but I'm missing the tier assessment, controls section, and HITL details that would be further down. I also don't have the Allium spec itself in context, though I can reference the policy document's requirements around human-in-the-loop processes from §8. I should be upfront about these gaps while working through what I do have available.

</details>

---

## Exchange 2

### Sarah

Focus on the HITL claim. Does the arrangement described in §5 of the submission meet HITLArrangement.is_substantive in the spec? Walk through each component.

### Copilot

## HITL Conformance: `HITLClaimsMustBeSubstantive`

---

### Retrieval Status — Before I Proceed

The `search_library` call returned the submission preamble and §1 again. **§5 of the submission (the HITL arrangement description) and the Allium spec file are still not in my context.** I will not reconstruct §5 from memory.

However — and this is material — **both compliance audit documents are in my context**, and the v1 audit's finding block contains a verbatim recitation of the invariant's four components. I can use that to define the test, then apply what the audits actually found.

---

### The Test: `HITLArrangement.is_substantive`

Per the v1 baseline audit finding against `HITLClaimsMustBeSubstantive` (LIWP-POL-AI-001 §8), `is_substantive` is `true` only when **all four of the following hold simultaneously:**

| # | Component | Field name (per audit) |
|---|---|---|
| 1 | The reviewer holds qualifications adequate to evaluate the AI output | `reviewer_qualified` |
| 2 | A positive act is required before the output is acted upon — passive non-objection is not sufficient | `requires_affirmative_action` |
| 3 | Expected review time is at least 15 seconds | `expected_review_seconds >= 15` |
| 4 | The information presented to the reviewer at the point of review is described | `information_at_review_described` |

This matches the prose standard in LIWP-POL-AI-001 §8: *"the AI Governance Office can determine whether the human reviewer is in a position to make an informed judgment, including the time available for review and the information presented at the point of review."*

All four components are conjunctive. A single `false` makes `is_substantive = false` and the invariant fires as **BLOCKING**.

---

### What the Audits Found

#### v1 Audit (baseline spec)

The v1 audit raised `HITLClaimsMustBeSubstantive` as a **BLOCKING** finding against the `advisor` codebase, citing `src/lighthouse/audit.py:1-70`, `src/lighthouse/app.py:108-143`, and `src/lighthouse/sessions.py:1-20`. The finding language is:

> *"The invariant requires that any submission asserting the defined_hitl control must have an HITLArrangement where is_substantive is true … The arrangement as described does not satisfy this."*

The audit did not specify *which* of the four components failed — the finding text I have does not break it down component-by-component. That granularity is either in the body of the audit report beyond the snippet retrieved, or was not recorded at that level.

#### v2 Audit (post-amendment spec)

The v2 audit's `advisor` section states flatly:

> *"The HITL arrangement does not meet the §8 substantiveness standard."*

The disposition is recorded as **awaiting review**. The v2 audit did not upgrade this to resolved — meaning no remediation has been accepted as of the v2 audit pass.

---

### Component-Level Gap Analysis

This is where I must be precise about what I can and cannot do:

| Component | Can I assess it? | Basis |
|---|---|---|
| `reviewer_qualified` | ❌ Not from context | §5 of submission not retrieved |
| `requires_affirmative_action` | ❌ Not from context | §5 of submission not retrieved |
| `expected_review_seconds >= 15` | ❌ Not from context | §5 of submission not retrieved |
| `information_at_review_described` | ❌ Not from context | §5 of submission not retrieved |

I cannot walk through each component against the submission text because **§5 has not been retrieved**. Doing so would require me to invent the submission's claims — which I will not do.

---

### What I Can Tell You With Confidence

1. **Both audit passes raised this as BLOCKING.** The invariant `HITLClaimsMustBeSubstantive` fired in v1 and was not cleared in v2.

2. **The v2 audit disposition is "awaiting review"** — the finding has not been resolved, remediated, or accepted as a risk exception.

3. **The prose policy (LIWP-POL-AI-001 §8) independently confirms the standard.** Even if the Allium spec were unavailable, the prose policy's requirement that the AI Governance Office be able to assess whether the reviewer is *"in a position to make an informed judgment"* sets a substantive bar that the audits found unmet.

---

### Recommended Next Step

To do the component-level walkthrough you asked for, I need §5 of the submission in context. Please verify the full submission document (`Meridian AI Scenario Advisor v1.0 — AI Deployment Submission-rev1.md`) is indexed with all sections, then re-run retrieval targeting `section 5 HITL`. I can also run `search_library` against `"HITL arrangement Lighthouse reviewer affirmative action"` if that would help surface it — your call.

<details><summary>thinking</summary>

The user wants me to focus on the HITL claim and check it against the spec's `HITLClaimsMustBeSubstantive` invariant and `HITLArrangement.is_substantive` components. 

From my retrieved context, I have:
1. The compliance audit findings (both v1 and v2) which reference the HITL issue
2. The prose policy §8
3. Partial submission details

However, I still don't have:
- The full §5 of the submission describing the HITL arrangement
- The full Allium spec file with the exact `HITLArrangement` entity definition and `HITLClaimsMustBeSubstantive` invariant text


- The search results returned the preamble and §1 of the submission again, not §5. And the Allium spec is still not in context.

What I DO have from the compliance audit snippets:
- From v1 audit: `HITLClaimsMustBeSubstantive` (LIWP-POL-AI-001 §8) finding that references: reviewer_qualified, requires_affirmative_action, expected_review_seconds >= 15, and information_at_review_described all hold.
- From v2 audit: "The HITL arrangement does not meet the §8 substantiveness standard."

So from the v1 audit snippet, I can infer the four components of `HITLArrangement.is_substantive`.

The v1 audit document actually spells out the invariant requirements: any submission asserting the defined_hitl control must have an HITLArrangement where is_substantive is true, which means all four conditions—reviewer_qualified, requires_affirmative_action, expected_review_seconds >= 15, and information_at_review_described—must hold. I can cite what the audit documents say about these components, but I need to be careful not to claim I've read §5 of the submission or the actual Allium spec.

The v2 audit finding is clear: the HITL arrangement doesn't meet the §8 substantiveness standard. I can walk through what the audit found and what the four criteria are based on the compliance documents, but I should be transparent that I don't have §5 of the submission itself to analyze line by line.

</details>

---

## Exchange 3

### Sarah

What about the model identifier — claude-sonnet-4-6 is named directly in §1. Is that a problem under the policy as it stands?

### Copilot

## Model Identifier — `AISystemsMaintainModelLedger` Conformance

---

### Short Answer

Yes. It is a **BLOCKING** finding under the v2 spec. The issue is not the *naming* of `claude-sonnet-4-6` in the submission — it is that the codebase implements the identifier as a **hardcoded constant with no ledger mechanism**.

---

### The Applicable Obligation

| Element | Detail |
|---|---|
| **Rule** | `AISystemsMaintainModelLedger` |
| **Policy anchor** | LIWP-POL-AI-001 §6.4 (as amended, v2 amendment A4) |
| **Open question on record** | `OQ-Model-Ledger` — the spec itself flags this as an unresolved design question |

The requirement is that a `ModelVersionLedgerEntry` mechanism must exist — a code path that creates, updates, or exposes a ledger entry for each model version in use.

---

### What the v2 Audit Found

Citing the v2 compliance audit directly:

> *"`claude-sonnet-4-6` is a hardcoded constant with no `ModelVersionLedgerEntry` mechanism. `config.py` itself notes this is deferred (`OQ-Model-Ledger`). No code path creates, updates or exposes a ledger entry."*

— **[BLOCKING]** `AISystemsMaintainModelLedger`, `src/lighthouse/config.py:12-13` and `src/lighthouse/config.py:1-8`

Three things are notable here:

1. **The submission's own codebase acknowledges the gap.** The deferral comment (`OQ-Model-Ledger`) in `config.py` is an in-code admission that the ledger mechanism was knowingly not implemented. This is not a missed requirement — it is a deferred one.

2. **The submission and the spec are in direct conflict on this point.** The submission asserts the system is ready for pilot (June 2026); the spec, as amended in v2, requires the ledger mechanism to be in place before that assertion is credible. There is no conditional pilot pathway visible in my context.

3. **`OQ-Model-Ledger` is a declared `open question` in the spec.** This means the spec's authors recognised the ledger design was unresolved at drafting time. However — critically — an open question in the spec does not suspend the *rule* that references it. `AISystemsMaintainModelLedger` still fires as BLOCKING. The open question records that the *implementation approach* is under discussion; it does not grant a waiver.

---

### Distinction: Submission vs. Codebase

The naming of `claude-sonnet-4-6` in §1 of the submission is **not itself the problem** — the submission is supposed to name the model. The problem is the *implementation*:

| Layer | Status | Problem? |
|---|---|---|
| Submission §1 — model named | `claude-sonnet-4-6` stated | ✅ No — this is required disclosure |
| `config.py:12-13` — model identifier in code | Hardcoded constant | ❌ Yes — no `ModelVersionLedgerEntry` |
| Ledger creation/update path | Does not exist | ❌ Yes — BLOCKING |

---

### Interaction With Other Blocking Findings

For the reviewer's awareness, this is the **third** BLOCKING finding from the v2 audit on the `advisor` codebase:

| # | Rule | Policy anchor | Status |
|---|---|---|---|
| 1 | `HITLClaimsMustBeSubstantive` | LIWP-POL-AI-001 §8 | Awaiting review |
| 2 | `AgenticSystemsRequireTraceLog` | LIWP-POL-AI-001 §5.4 (AMEND v2 A3) | Per prior retrieval |
| 3 | `AISystemsMaintainModelLedger` | LIWP-POL-AI-001 §6.4 (AMEND v2 A4) | Awaiting review |

All three are BLOCKING. Under LIWP-POL-AI-001 §9, departures from §5 requirements require written approval from the AI Governance Committee with a documented business case and defined re-review date — but §9 covers exceptions to §5 controls specifically. Whether it extends to §6.4 and §8 obligations is not resolved in the text I have. That is worth the reviewer confirming before treating §9 as a clearance route for any of these three findings.

<details><summary>thinking</summary>

The user is asking about whether the hardcoded model identifier `claude-sonnet-4-6` is a problem under the policy. I have clear evidence from the v2 compliance audit about `AISystemsMaintainModelLedger` being a BLOCKING finding. Let me structure this clearly.

</details>

---
